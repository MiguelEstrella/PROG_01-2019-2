@model List<Pokemon>
    <h1>Lista de pokemones</h1>
    @if(Model.Count == 0){
        <p>No hay pokemones registrados</p>
    }

    @foreach (var p in Model)
    {
        <h2>@p.Nombre</h2>
        <img scr="@p.Foto">
    }